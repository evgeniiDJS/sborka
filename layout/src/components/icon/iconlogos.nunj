{% macro iconlogos(props) %}
	<svg{% if props.className %} class="{{ props.className }}"{% endif %}{% if props.attributes %} {{ props.attributes }}{% endif %}>
		<use xlink:href="#iconlogos-{{ props.iconName }}" />
	</svg>
{% endmacro %}
